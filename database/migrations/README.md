# database/migrations/

Prisma-managed migrations only (025_DATABASE_MIGRATIONS.md).
Do NOT place raw SQL files here manually — use `pnpm db:migrate`.
The Canonical Data Model schema arrives in the Database sprint.
