/**
 * Next.js streaming loading UI (locale-scoped).
 * Replaced by real skeleton components in the Dashboard sprint.
 */
export default function Loading() {
  return (
    <div className="flex min-h-screen items-center justify-center">
      <span className="animate-pulse text-sm text-neutral-400">Loading…</span>
    </div>
  );
}
